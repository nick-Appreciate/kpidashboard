import PlaidLinkPanel from '../../../components/PlaidLinkPanel';

export const metadata = {
  title: 'Link a Bank via Plaid - Appreciate Dashboard',
};

export default function PlaidLinkPage() {
  return <PlaidLinkPanel />;
}
